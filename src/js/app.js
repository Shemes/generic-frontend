require('./vendor');

document.addEventListener('DOMContentLoaded', () => {
    console.info('STARTER KIT: JS Ready to go!');
});


$('document').ready(() => console.info('JQUERY is also working...'));
